---
title: 'MDX示例'
summary: '本文提供了MDX语法的示例。'
translationKey: 'mdx'
date: '2025-11-11T07:40:00Z'
tags: ['markdown', '指南', 'jsx', 'mdx', '数学公式']
categories: ['示例']
language: 'zh'
isCanonical: true
---

## JSX

### 内置目录组件

```jsx
// 内置目录组件
// "内置目录组件"章节会被排除

<TOCInline toc={props.toc} exclude="内置目录组件" toHeading={3} />
```

<TOCInline toc={props.toc} exclude="内置目录组件" toHeading={3} />

### 内置图片组件

`./components/Image.tsx` 实现了对 WebP 图片的优先加载及自动回退功能，在现代浏览器中可实现更高效的图片加载体验。加载过程中采用 SVG 占位符提升用户体验。在开发服务器启动或生产构建时，`genwebp`脚本会通过 Node.js 的 `worker_threads` 多线程机制，自动生成本地 PNG 和 JPG 格式图片的 WebP 副本。

本地 PNG 和 JPG 格式的文章 Banner 图片无需手动设置 WebP 副本的路径。

```jsx
// 可以使用Tailwind CSS
<div className="container mx-auto p-4">
  <h3 className="text-center">Unsplash 图库</h3>
  <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
    <div className="group relative overflow-hidden rounded-lg bg-neutral-100 drop-shadow-md transition-shadow duration-300 ease-in-out hover:shadow-xl dark:bg-gray-900">
      <Image
        src="/static/images/canada/mountains.jpg"
        alt="山脉"
        className="h-50 w-full object-cover object-center transition-transform duration-300 ease-in-out group-hover:scale-105"
        loading="lazy"
        showPlaceholder={true}
      />
      <p className="text-center">山脉</p>
    </div>
    <div className="group relative overflow-hidden rounded-lg bg-neutral-100 drop-shadow-md transition-shadow duration-300 ease-in-out hover:shadow-xl dark:bg-gray-900">
      <Image
        src="/static/images/canada/toronto.jpg"
        alt="多伦多"
        className="h-50 w-full object-cover object-center transition-transform duration-300 ease-in-out group-hover:scale-105"
        loading="lazy"
        showPlaceholder={true}
      />
      <p className="text-center">多伦多</p>
    </div>
  </div>
</div>
```

<div className="container mx-auto p-4">
  <h3 className="text-center">Unsplash 图库</h3>
  <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
    <div className="group relative overflow-hidden rounded-lg bg-neutral-100 drop-shadow-md transition-shadow duration-300 ease-in-out hover:shadow-xl dark:bg-gray-900">
      <Image
        src="/static/images/canada/mountains.jpg"
        alt="山脉"
        className="h-50 w-full object-cover object-center transition-transform duration-300 ease-in-out group-hover:scale-105"
        loading="lazy"
        showPlaceholder={true}
      />
      <p className="text-center">山脉</p>
    </div>
    <div className="group relative overflow-hidden rounded-lg bg-neutral-100 drop-shadow-md transition-shadow duration-300 ease-in-out hover:shadow-xl dark:bg-gray-900">
      <Image
        src="/static/images/canada/toronto.jpg"
        alt="多伦多"
        className="h-50 w-full object-cover object-center transition-transform duration-300 ease-in-out group-hover:scale-105"
        loading="lazy"
        showPlaceholder={true}
      />
      <p className="text-center">多伦多</p>
    </div>
  </div>
</div>

山脉照片来自 [John
Lee](https://unsplash.com/@john_artifexfilms?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText)

多伦多照片来自 [Matthew
Henry](https://unsplash.com/@matthewhenry?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText)

## GitHub 风格提示框

```md
> [!NOTE]
> 用户即使快速浏览内容，也应该了解的有用信息。

> [!TIP]
> 提供更好或更轻松完成任务的有用建议。

> [!IMPORTANT]
> 用户需要知道的关键信息，以实现其目标。

> [!WARNING]
> 需要用户立即注意的紧急信息，以避免问题。

> [!CAUTION]
> 提醒某些操作可能带来的风险或负面结果。
```

> [!NOTE]
> 用户即使快速浏览内容，也应该了解的有用信息。

> [!TIP]
> 提供更好或更轻松完成任务的有用建议。

> [!IMPORTANT]
> 用户需要知道的关键信息，以实现其目标。

> [!WARNING]
> 需要用户立即注意的紧急信息，以避免问题。

> [!CAUTION]
> 提醒某些操作可能带来的风险或负面结果。

## 脚注

```plaintext
这是一个脚注[^1]

这是第二个脚注[^2]

[^1]: 参考文献1


[^2]:
    要在脚注中添加换行，请在行末添加两个空格
    这是第二行
```

这是一个脚注[^1].

支持多行脚注[^2].

[^1]: 参考文献1

[^2]:
    要在脚注中添加换行，请在行末添加两个空格  
    这是第二行

## 代码高亮

代码高亮由 [`rehype-pretty-code`](https://rehype-pretty.pages.dev/) 实现。

### 带标题与行号的代码块

````
```ts title="utils/types.ts" showLineNumbers
export interface User {
  id: number
  name: string
  email: string
}

export type UserRole = 'admin' | 'user' | 'guest'
```
````

```ts title="utils/types.ts" showLineNumbers
export interface User {
  id: number
  name: string
  email: string
}

export type UserRole = 'admin' | 'user' | 'guest'
```

### Diff

```diff title="package.json changes"
{
  "name": "my-app",
  "version": "1.0.0",
- "dependencies": {
-   "react": "^17.0.0"
- },
+ "dependencies": {
+   "react": "^18.0.0",
+   "react-dom": "^18.0.0"
+ },
  "devDependencies": {
    "typescript": "^4.5.0"
  }
}
```

## 数学公式

数学公式渲染采用 [KaTeX](https://katex.org/)

行内公式 `$E = mc^2$`: $E = mc^2$

块级公式:

```plaintext
$$
\int_{-\infty}^{\infty} e^{-x^2} \, dx = \sqrt{\pi}
$$
```

$$
\int_{-\infty }^{\infty} e^{-x^2} \, dx = \sqrt{\pi}
$$

矩阵:

```plaintext
$$
\begin{bmatrix}
a & b \\
c & d
\end{bmatrix}
$$
```

$$
\begin{bmatrix}
a & b \\
c & d
\end{bmatrix}
$$
