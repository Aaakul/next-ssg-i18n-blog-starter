---
title: 'Feature Demo'
translationKey: 'invalid-translationKey'
date: '2025-11-11T15:30:00+08:00'
language: 'en'
---

## Post summary fallback

If there is no `summary` in the `frontmatter`, the TOC will be used as a fallback.
If there is no valid TOC, `title` will be used as the final fallback.

## Language switch fallback

This post does not have a Chinese or Japanese version linked via `translationKey`. You can try switching languages to view the language fallback modal.

```mdx
---
title: 'Feature Demo'
translationKey: 'invalid-translationKey'
date: '2025-11-11T15:30:00+08:00'
language: 'en'
---

## Post summary fallback

If there is no `summary` in the `frontmatter`, the TOC will be used as a fallback.
If there is no valid TOC, `title` will be used as the final fallback.

## Language switch fallback

This post does not have a Chinese or Japanese version linked via `translationKey`. You can try switching languages to view the language fallback modal.
```
